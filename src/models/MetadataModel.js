const Access = require('../enums/access')

const MetadataModel = {
    startDate: null,
    endDate: null,
    name: null,
    description: null,
    userId: null,
    access: Access.Private
}

module.exports = MetadataModel;